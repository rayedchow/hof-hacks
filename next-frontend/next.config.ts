/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

// This is the right way to change the default port in Next.js
// To use port 8080, run:
// next dev -p 8080
// OR update your package.json scripts:
// "dev": "next dev -p 8080"

export default nextConfig;
